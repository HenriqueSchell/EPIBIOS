# Fotos e links

Os ajustes ficam em `index.html`.

- Fotos: altere o `src` das imagens dos cartões. Luiza tem um bloco `photo-placeholder`; substitua por uma imagem com a classe `member-photo`.
- Perfis: procure `data-profile="nome-lattes"` ou `data-profile="nome-linkedin"`. Insira `href="URL_OFICIAL"`, `target="_blank"` e `rel="noopener noreferrer"`, e remova `aria-disabled="true"`.
- Coordenação: os identificadores são `roberta-lattes`, `roberta-orcid` e `roberta-scholar`.
- Resultado da competição: preencha os dois pontos `cardihack-resultados` e `cardihack-resultados-destaque` com a mesma página oficial.
- Artigos: a seção `producao-cientifica` está preparada para receber título, autores, periódico, ano e link DOI ou página oficial. Não havia referências bibliográficas completas no site ou no documento, portanto nenhuma foi inventada.

Os links Lattes já associados corretamente a Roberta e Henrique foram preservados. Os outros perfis ficaram sem destino; os cartões anteriores repetiam o Lattes de Henrique para outros integrantes.

Os arquivos existentes foram mantidos no disco; os botões de download foram removidos da página. Resumos, pôsteres, apresentações, projetos e premiações devem permanecer como conteúdo na página, sem anexos.
